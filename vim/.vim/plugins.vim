call plug#begin('~/.vim/plugged')

Plug 'morhetz/gruvbox'
Plug 'vim-airline/vim-airline'
Plug 'yggdroot/indentline'

call plug#end()

" Enter those command in command mode. A new editor opens that executes the
" job. It can be quit like a normal file in vim. First save and reload the
" file before executing comands.

"PlugInstall
""PlugUpdate
"PlugStatus
""PlugClean
"PlugUpgrade
